<?php

declare(strict_types=1);

use ReceiverControl\Command\Device\Info;
use ReceiverControl\Command\SetAllZoneStereo;

$commands = [
    SetAllZoneStereo::class => 'AllZoneStereo',
    Info::class => 'Info',
];
?>

<div class="jumbotron">
    <div class="row">
        <div class="col">
            <div class="btn-group">
                <?php
                foreach ($commands as $command => $label) {
                    echo sprintf('<button class="switchable btn btn-outline-secondary" data-command-on-click="%s">%s</button>',
                        $command,
                        $label
                    );
                }
                ?>
            </div>
        </div>
        <div class="col text-right">

        </div>
    </div>
</div>
