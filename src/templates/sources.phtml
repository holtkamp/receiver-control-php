<?php

declare(strict_types=1);

use ReceiverControl\Command\Source\Inputs;
use ReceiverControl\Command\Source\Select;

?>
<div class="jumbotron">
    <div class="row">
        <div class="col">
            <div class="btn-group">
                <?php
                foreach (Inputs::getSourceInputs() as $sourceInputKey => $sourceInputValue) {
                    echo sprintf('<button class="switchable btn btn-outline-secondary" data-command-on-click="%s" data-source-input="%s">%s</button>',
                        Select::class,
                        $sourceInputKey,
                        $sourceInputValue
                    );
                }
                ?>
            </div>
        </div>
        <div class="col text-right">

        </div>
    </div>
</div>
